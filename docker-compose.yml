version: '3.5'

networks:
  web:
    external: true

services:
  gab:
    container_name: gab
    build: .
    restart: always
    env_file:
      - .env
    ports:
      - "3000:3000"
    networks:
      - web